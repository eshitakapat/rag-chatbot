version: "3.9"

services:
  api:
    build: ./backend
    container_name: rag-api
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: always

  frontend:
    build: ./frontend
    container_name: rag-frontend
    ports:
      - "3000:80"
    restart: always
